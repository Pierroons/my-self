# Composer
/vendor/
composer.lock

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache

# Le runtime de la démo (base, clés, escrow) est ignoré par
# demo/selfdataguard/.gitignore depuis le rangement du 17/08/2026 :
# les règles vivent avec les fichiers qu'elles protègent.

# Playground runtime — DB + server-side secrets (never committed).
# Dossier IGNORÉ EN ENTIER : les patterns partiels laissaient passer
# admin-recovery.key / .pub / mini_db_escrow.php (secrets de scratch).
/playground/

# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
