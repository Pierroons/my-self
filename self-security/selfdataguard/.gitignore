# Composer
/vendor/
composer.lock

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache

# Demo runtime — DB and server-side secrets MUST NEVER be committed
/demo/storage/*.sqlite
/demo/storage/*.db
/demo/storage/*.sqlite-journal
/demo/storage/*.sqlite-wal
/demo/storage/*.sqlite-shm
/demo/storage/*.bin
/demo/storage/blindkey.*
/demo/storage/.env
# Admin recovery keys (public key + passphrase-sealed private key = SECRET)
/demo/storage/admin-recovery.*

# Playground runtime — DB + server-side secrets (never committed).
# Dossier IGNORÉ EN ENTIER : les patterns partiels laissaient passer
# admin-recovery.key / .pub / mini_db_escrow.php (secrets de scratch).
/playground/

# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
