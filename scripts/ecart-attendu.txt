# Écarts légitimes entre le dépôt et l'instance — lus par ecart-instance.sh.
#
# Format :   <regex du chemin>  <TAB>  <raison>
#
# La raison n'est pas décorative, même règle que l'allowlist OPSEC : une
# exception sans justification devient indistinguable d'un oubli, et six mois
# plus tard personne ne sait si l'écart est voulu ou s'il est passé au travers.
#
# ⚠️ Un écart déclaré ici reste affiché, en ATTENDU. On ne le masque pas — on
# dit qu'on le connaît. Masquer un écart, c'est se préparer à ne plus le voir
# le jour où il change de nature.

# 🔑 Retiré le 24/08/2026 : `^self-right/selfjustice/site/index\.html$`, unique
# entrée du fichier, et orpheline deux fois. Elle visait `index.html` quand le
# fichier versionné s'appelle `index.php` — aucun fichier ne pouvait lui
# correspondre. Et le phénomène qu'elle décrivait n'existe plus : `update_stats.sh`
# a cessé le 19/08 d'écrire dans le code de production, il ne touche que ses
# données, et la page compose ses chiffres au moment où elle est servie.
#
# Une exception survit à ce qu'elle excuse sans que rien ne le signale. Celle-ci
# a tenu cinq jours après la disparition de son motif, et un renommage de plus.
# Un fichier sans entrée est un fichier honnête : la sonde n'excuse rien.
